# Boombastis - Sticky Post

Append plugin defined template as additional post in a page. Template added by calling `boom_add_sticky_post(string $viewtype)` into theme template. Additional sticky template can be created on plugin directory by creating new files called `post-{$viewtype}.php`.